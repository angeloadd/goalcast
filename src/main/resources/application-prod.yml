server:
    servlet:
        session:
            cookie:
                secure: true

logging:
    structured:
        format:
            console: ecs
    level:
        root: INFO
        com.goalcast: DEBUG
        org.springframework.security: INFO
