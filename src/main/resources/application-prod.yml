server:
    servlet:
        session:
            cookie:
                secure: true

logging:
    structured:
        format:
            console: logfmt
    level:
        root: INFO
        com.goalcast: DEBUG
        org.springframework.security: INFO
